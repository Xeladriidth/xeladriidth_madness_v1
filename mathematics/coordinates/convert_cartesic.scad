function convert_cartesic(input_data,pointer,carry) = 

assert(false,"function definition overrun for function convert_cartesic");

//does this function require its own assessments for determining input type?

//a) pipe input type through; slow, difficult or needs header

//b) write assertions

//c) cry//unfinished
