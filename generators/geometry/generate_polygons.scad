function generate_polygons(input_geometry,pointer,carry) =

/*

(
  (len(carry[0]>=3)
)?
    carry:

*/

undef;

test_list_1 = [

for(a=[0:1:3])
for(b=[0:1:3])
for(c=[0:1:3])

[a,b,c]
];

echo(test_list_1);//unfinished
