function field_concat(input_field) = 

//goal of function: takes an input field and some instructions to locate carry within field, then add something to it;
//therefore needs informations of what to add to where

false;//unfinished
