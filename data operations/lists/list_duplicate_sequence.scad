//directie: have two consecutive copies of input list one after another

function list_duplicate_sequence(input_list) =

assert(is_list(input_list))

concat([input_list],[input_list]);//unfinished
