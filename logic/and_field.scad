function and_field(input_field) = 

//function takes an input field of bools and performs an and-check on all these elements;

//optionality: check for presence of string "bool" in input field as additional assertion

false;//unfinished
